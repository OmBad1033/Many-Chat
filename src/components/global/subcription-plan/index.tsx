import React from "react";

type Props = {
  type: "FREE" | "PRO";
  children: React.ReactNode;
};

function SubcriptionPlan({ children, type }: Props) {
  //wip: return the subcription plan
  return children;
}

export default SubcriptionPlan;
